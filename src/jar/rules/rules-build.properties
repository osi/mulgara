#
# Properties used by the Rules module
#
# $Id: rules-build.properties,v 1.1 2005/06/26 12:42:42 pgearon Exp $
#

# Module Name
rules.name          = Rules

# General module properties
rules.conf.dir      = ${conf.dir}
rules.src.dir       = ${jar.src.dir}/rules
rules.obj.dir       = ${jar.obj.dir}/rules
rules.dist.dir      = ${bin.dir}
rules.test.dir      = ${test.dir}/rules
rules.jxtest.dir    = ${jxtest.dir}/rules
rules.jar           = rules-base-${mulgara-version}.jar
